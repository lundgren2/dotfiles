remove-node_modules() {
  echo "removing node_modules:"
  find . -name 'node_modules' -type d -prune

  if read -q "Are you sure you want to remove?"; then
      find . -name 'node_modules' -type d -prune -exec rm -rf {} \;
  else
      echo
      echo "'$choice' not 'Y' or 'y'. Exiting..."
  fi
}
